<%= form_with(model: demoimg) do |form| %>
  <% if demoimg.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(demoimg.errors.count, "error") %> prohibited this demoimg from being saved:</h2>

      <ul>
        <% demoimg.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="field">
    <%= form.label :img %>
    <%= form.text_area :img %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
